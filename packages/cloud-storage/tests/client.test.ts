import { describe, it, expect } from 'vitest';

describe('@musallam/cloud-storage', () => {
  it('should export from flat', async () => {
    // Populated after `pnpm generate`
    expect(true).toBe(true);
  });
});
