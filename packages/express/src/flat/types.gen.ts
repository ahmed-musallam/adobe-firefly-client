// This file is auto-generated by @hey-api/openapi-ts

export type ClientOptions = {
    baseUrl: 'https://express-api.adobe.io' | (string & {});
};

/**
 * Represents the sortBy field enum for tagged documents.
 */
export type TaggedDocumentsSortBy = '+modifiedDate' | '+name' | '-modifiedDate' | '-name' | '-relevance' | 'modifiedDate' | 'name' | 'relevance';

/**
 * Response payload for GET "/tagged-documents" API.
 */
export type TaggedDocumentsResponse = {
    paging?: Pagination;
    /**
     * A list of documents accessible (both owned and shared with) to the user.
     */
    documents?: Array<ExpressDocument>;
};

/**
 * Details of the error response.
 */
export type ErrorResponseDetails = {
    /**
     * The error code.
     */
    error_code: string;
    /**
     * The error message.
     */
    message?: string;
    /**
     * Validation error messages.
     */
    validation_errors?: Array<string>;
};

/**
 * Response payload for GET "/tagged-documents/\{documentId\}" API.
 */
export type TaggedDocumentDetailsResponse = {
    /**
     * The name of the document.
     */
    name: string;
    /**
     * The unique identifier of the document.
     */
    id: string;
    paging?: Pagination;
    /**
     * List of the pages from Express Document.
     */
    documentPages: Array<DocumentPageDetails>;
};

/**
 * Generate variation of a tagged document. One single variation can be generated at a time.
 */
export type GenerateVariationRequest = {
    /**
     * The identifier of the tagged document.
     */
    id: string;
    variationDetails: VariationDetails;
};

/**
 * Response payload returned by any asynchronous endpoint.
 */
export type JobResponse = {
    /**
     * The identifier of the job.
     */
    jobId: string;
    /**
     * A URL using which the status of the job can be fetched.
     */
    statusUrl: string;
};

/**
 * Payload for requesting rendition of a document.
 */
export type ExportRenditionRequest = {
    /**
     * The identifier of the document that needs to be rendered.
     */
    id: string;
    /**
     * A comma-separated string of page numbers for which the rendition request is made. For example, "1,2,3".
     * Page ranges can also be specified. For example, "1-3" includes pages 1, 2, and 3.
     * Another example is "1,3-5", which includes pages 1, 3, 4, and 5.
     * "1-" can be used to specify all pages, while "5-" indicates page 5 to the last page.
     * If not provided, the first page is considered by default. Page numbers start from 1.
     */
    pages?: string;
    /**
     * Options for the requested rendition.
     */
    options: ImageRenditionOptions | VideoRenditionOptions | PdfRenditionOptions;
};

/**
 * Base response payload returned by Job Status API.
 */
export type JobStatusResponse = {
    /**
     * The identifier of the job.
     */
    jobId: string;
    status: JobStatus;
};

/**
 * Response payload for the "export-rendition" API.
 */
export type ExportRenditionResponse = PageRenditionResponse | PdfRenditionResponse;

/**
 * Response payload for the "generate-variation" API.
 */
export type GenerateVariationResponse = {
    document: ExpressDocument;
    /**
     * The identifier of the job.
     */
    jobId: string;
    status: JobStatus;
};

/**
 * Pagination details of the response.
 */
export type Pagination = {
    /**
     * A URL using which the next page of the response can be fetched.
     */
    nextUrl: string;
    /**
     * Total number of items available that match the query.
     */
    totalRecords: number;
};

/**
 * Details of the Express document.
 */
export type ExpressDocument = {
    /**
     * The unique identifier of the document.
     */
    id: string;
    /**
     * The name or title of the document.
     */
    name: string;
    /**
     * Pre-signed URL for the Document's thumbnail rendition.
     * The size of the rendition will be `640 pixels` along the longest side. The format of the rendition will be a `PNG` image.
     */
    thumbnailUrl: string;
};

/**
 * The details of a single page in the Express Document.
 */
export type DocumentPageDetails = {
    /**
     * The number of the page starting from 1.
     */
    pageNumber: number;
    /**
     * The title of the page.
     */
    pageTitle: string;
    /**
     * Pre-signed URL for the page's thumbnail rendition valid for 4 hours.
     * The size of the rendition will be 640 pixels along the longest side. The format of the rendition will be a PNG image.
     */
    thumbnailUrl: string;
    size: Size;
    /**
     * List of all the tagged elements on this page.
     * If this page do not have any tagged element, this field will be undefined.
     */
    taggedElements?: Array<TaggedElement>;
};

/**
 * Represents a dimension within a Page.
 */
export type Size = {
    /**
     * The width in pixels ( px ).
     */
    width: number;
    /**
     * The height in pixels ( px ).
     */
    height: number;
};

/**
 * The details of a tag element inside the page.
 */
export type TaggedElement = {
    /**
     * The name of the tag. Following rules apply to the tag name:
     *
     * 1. In the same page, tag names should be unique.
     * 2. Across different pages, tags of same element type can have same name.
     * 3. The tag name is case-sensitive.
     */
    name: string;
    type: TaggedElementType;
    position: Point;
    size: Size;
};

/**
 * The supported tag types in a document.
 */
export type TaggedElementType = 'image' | 'text' | 'video';

/**
 * Represents position within a page. 0,0 represents the top-left corner of a page.
 */
export type Point = {
    /**
     * The left co-ordinate position in px.
     */
    x: number;
    /**
     * The top co-ordinate position in px.
     */
    y: number;
};

/**
 * Details of how the variation should be generated.
 */
export type VariationDetails = {
    /**
     * A key-value pair where each key represents the unique identifier of a tag element, and the value represents the corresponding element's content. The value type varies by element type: for text elements, it is a plain string; for image or video elements, it is a pre-signed URL pointing to the image or video resource. Allowed domains for pre-signed URLs are AWS, Dropbox and Azure (windows.net). The key is obtained from the response of `/tagged-documents/{documentId}` in `documentPages` -\> `taggedElements` -\> `name`.
     */
    tagMappings: {
        [key: string]: string;
    };
    /**
     * A comma-separated string of page numbers for which the variation request is made. For example, "1,2,3".
     * Page ranges can also be specified. For example, "1-3" includes pages 1, 2, and 3.
     * Another example is "1,3-5", which includes pages 1, 3, 4, and 5.
     * "1-" can be used to specify all pages, while "5-" indicates page 5 to the last page.
     * If not provided, all pages are considered by default. Page numbers start from 1.
     */
    pages?: string;
    /**
     * The preferred name for the generated document. If you do not provide a name or the name is already in use, the system automatically generates a unique name.
     */
    preferredDocumentName?: string;
    /**
     * The identifier of the project where the variation is to be stored.
     */
    projectId?: string;
};

/**
 * Options for the image rendition request.
 */
export type ImageRenditionOptions = {
    format: ImageRenditionFormat;
    /**
     * The size of the "image" rendition on the longest side. The aspect ratio is maintained.
     * For image the minimum size supported is 1px and maximum size supported is 8192px.
     *
     * If not provided, the default size of page is considered.
     */
    size?: number;
};

/**
 * The format of the bitmap rendition.
 */
export type ImageRenditionFormat = 'image/jpeg' | 'image/png';

/**
 * Options for the video rendition request.
 */
export type VideoRenditionOptions = {
    /**
     * video rendition format
     */
    format: 'video/mp4';
    /**
     * The size of the "video" rendition on the longest side. The aspect ratio is maintained.
     * For video the minimum size supported is 146px and maximum size supported is 4096px.
     *
     * If not provided, the default size of page is considered.
     */
    size?: number;
};

/**
 * Options for the requested PDF rendition.
 */
export type PdfRenditionOptions = {
    /**
     * PDF rendition format
     */
    format: 'application/pdf';
    /**
     * The type of PDF rendition. Default is "standard".
     */
    pdfType?: 'print' | 'standard';
    /**
     * Controls whether pages are downloaded as separate PDF files.
     * When true, each page is downloaded as its own PDF file.
     * When false, all pages are combined into a single PDF file.
     *
     * Defaults to false.
     */
    downloadIndividualPdfFiles?: boolean;
    /**
     * Configuration for the provided PDF type.
     */
    config?: PrintConfig | StandardConfig;
};

/**
 * Configuration options for print-ready PDF renditions.
 */
export type PrintConfig = {
    /**
     * Controls whether to include bleed settings in the PDF.
     * Defaults to false.
     */
    bleed?: boolean;
    bleedSettings?: ValueWithUnit;
    /**
     * Controls whether to include crop margins in the PDF.
     * Defaults to false.
     */
    cropMargins?: boolean;
    cropMarginsSettings?: ValueWithUnit;
};

/**
 * Configuration settings for the "print" PDF rendition.
 * It includes values to adjust the amount and unit of measurement.
 */
export type ValueWithUnit = {
    /**
     * The amount of bleed or crop margin.
     * It can be a positive or negative value.
     */
    amount: number;
    unit: PdfMarginUnit;
};

/**
 * The unit of measurement for the bleed or crop margin.
 */
export type PdfMarginUnit = 'in' | 'mm';

/**
 * Configuration settings for standard PDF rendition.
 */
export type StandardConfig = {
    /**
     * Controls whether accessibility tags are included in the PDF.
     * When true, accessibility tags will be embedded in the PDF.
     * When false, accessibility tags will be omitted.
     *
     * Defaults to false.
     */
    accessibilityTags?: boolean;
};

/**
 * Various statuses of a job.
 */
export type JobStatus = 'cancel_requested' | 'cancelled' | 'failed' | 'partially_succeeded' | 'pending' | 'running' | 'succeeded';

/**
 * Response payload for the "export-rendition" API when exporting page renditions.
 */
export type PageRenditionResponse = {
    /**
     * The identifier of the document for which the rendition request was made.
     */
    id: string;
    /**
     * The exported renditions outcome indicates whether they were successful or failed.
     */
    pageRenditionsResult: Array<SuccessfulPageRendition | FailedPageRendition>;
    /**
     * The identifier of the job.
     */
    jobId: string;
    status: JobStatus;
};

/**
 * Details of the page rendition that was successfully exported.
 */
export type SuccessfulPageRendition = {
    /**
     * The page number for which the rendition export request was made.
     */
    pageNumber: number;
    /**
     * Pre-signed URL where the rendition result can be downloaded from.
     */
    renditionUrl: string;
};

/**
 * Details of the page rendition that failed to export.
 */
export type FailedPageRendition = {
    /**
     * The page number for which the rendition export request was made.
     */
    pageNumber: number;
    error: ErrorResponseDetails;
};

/**
 * Response payload for the "export-rendition" API when exporting PDF rendition.
 */
export type PdfRenditionResponse = {
    /**
     * The identifier of the document for which the rendition request was made.
     */
    id: string;
    /**
     * The exported PDF rendition outcome.
     */
    pageRenditionsResult: Array<SuccessfulPdfRendition | FailedPdfRendition>;
    /**
     * The identifier of the job.
     */
    jobId: string;
    status: JobStatus;
};

/**
 * Result of a successful PDF rendition export.
 */
export type SuccessfulPdfRendition = {
    /**
     * The URL of the PDF rendition.
     */
    renditionUrl: string;
};

/**
 * Result of a failed PDF rendition export.
 */
export type FailedPdfRendition = {
    error: ErrorResponseDetails;
};

export type TaggedDocumentsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * Pagination start index. Default is 0.
         */
        start?: number;
        /**
         * Pagination limit. Default is 10 and maximum value allowed is 25.
         */
        limit?: number;
        /**
         * Use `sortBy` to sort the response.
         */
        sortBy?: TaggedDocumentsSortBy;
    };
    url: '/beta/tagged-documents';
};

export type TaggedDocumentsErrors = {
    /**
     * Bad Request
     */
    400: ErrorResponseDetails;
    /**
     * Unauthorized
     */
    401: ErrorResponseDetails;
    /**
     * Forbidden
     */
    403: ErrorResponseDetails;
    /**
     * Not Found
     */
    404: ErrorResponseDetails;
    /**
     * Unprocessable Entity
     */
    422: ErrorResponseDetails;
    /**
     * Too Many Requests
     */
    429: ErrorResponseDetails;
    /**
     * Internal Server Error
     */
    500: ErrorResponseDetails;
    /**
     * Service Unavailable
     */
    503: ErrorResponseDetails;
};

export type TaggedDocumentsError = TaggedDocumentsErrors[keyof TaggedDocumentsErrors];

export type TaggedDocumentsResponses = {
    /**
     * Successfully retrieved the tagged documents.
     */
    200: TaggedDocumentsResponse;
};

export type TaggedDocumentsResponse2 = TaggedDocumentsResponses[keyof TaggedDocumentsResponses];

export type TaggedDocumentDetailsData = {
    body?: never;
    path: {
        /**
         * The unique identifier of the document.
         */
        documentId: string;
    };
    query?: {
        /**
         * Specifies the starting document page number from which details need to be fetched. Default is 1.
         */
        start?: number;
    };
    url: '/beta/tagged-documents/{documentId}';
};

export type TaggedDocumentDetailsErrors = {
    /**
     * Bad Request
     */
    400: ErrorResponseDetails;
    /**
     * Unauthorized
     */
    401: ErrorResponseDetails;
    /**
     * Unprocessable Entity
     */
    422: ErrorResponseDetails;
    /**
     * Internal Server Error
     */
    500: ErrorResponseDetails;
};

export type TaggedDocumentDetailsError = TaggedDocumentDetailsErrors[keyof TaggedDocumentDetailsErrors];

export type TaggedDocumentDetailsResponses = {
    /**
     * Successfully retrieved the details of the tagged document.
     */
    200: TaggedDocumentDetailsResponse;
};

export type TaggedDocumentDetailsResponse2 = TaggedDocumentDetailsResponses[keyof TaggedDocumentDetailsResponses];

export type GenerateVariationData = {
    body: GenerateVariationRequest;
    path?: never;
    query?: never;
    url: '/beta/generate-variation';
};

export type GenerateVariationErrors = {
    /**
     * Bad Request
     */
    400: ErrorResponseDetails;
    /**
     * Unauthorized
     */
    401: ErrorResponseDetails;
    /**
     * Forbidden
     */
    403: ErrorResponseDetails;
    /**
     * Not Found
     */
    404: ErrorResponseDetails;
    /**
     * Unprocessable Entity
     */
    422: ErrorResponseDetails;
    /**
     * Too Many Requests
     */
    429: ErrorResponseDetails;
    /**
     * Internal Server Error
     */
    500: ErrorResponseDetails;
    /**
     * Service Unavailable
     */
    503: ErrorResponseDetails;
};

export type GenerateVariationError = GenerateVariationErrors[keyof GenerateVariationErrors];

export type GenerateVariationResponses = {
    /**
     * The request has been accepted and the job is in progress.
     */
    202: JobResponse;
};

export type GenerateVariationResponse2 = GenerateVariationResponses[keyof GenerateVariationResponses];

export type ExportRenditionData = {
    body: ExportRenditionRequest;
    path?: never;
    query?: never;
    url: '/beta/export-rendition';
};

export type ExportRenditionErrors = {
    /**
     * Bad Request
     */
    400: ErrorResponseDetails;
    /**
     * Unauthorized
     */
    401: ErrorResponseDetails;
    /**
     * Forbidden
     */
    403: ErrorResponseDetails;
    /**
     * Not Found
     */
    404: ErrorResponseDetails;
    /**
     * Unprocessable Entity
     */
    422: ErrorResponseDetails;
    /**
     * Too Many Requests
     */
    429: ErrorResponseDetails;
    /**
     * Internal Server Error
     */
    500: ErrorResponseDetails;
    /**
     * Service Unavailable
     */
    503: ErrorResponseDetails;
};

export type ExportRenditionError = ExportRenditionErrors[keyof ExportRenditionErrors];

export type ExportRenditionResponses = {
    /**
     * The request has been accepted and the job is in progress.
     */
    202: JobResponse;
};

export type ExportRenditionResponse2 = ExportRenditionResponses[keyof ExportRenditionResponses];

export type GetJobStatusData = {
    body?: never;
    path: {
        /**
         * The unique identifier of the job.
         */
        jobId: string;
    };
    query?: never;
    url: '/status/{jobId}';
};

export type GetJobStatusErrors = {
    /**
     * Not Found
     */
    404: ErrorResponseDetails;
    /**
     * Internal Server Error
     */
    500: ErrorResponseDetails;
};

export type GetJobStatusError = GetJobStatusErrors[keyof GetJobStatusErrors];

export type GetJobStatusResponses = {
    /**
     * Successful response with job status and details.
     */
    200: JobStatusResponse | ExportRenditionResponse | GenerateVariationResponse;
};

export type GetJobStatusResponse = GetJobStatusResponses[keyof GetJobStatusResponses];
