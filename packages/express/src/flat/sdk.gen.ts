// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client';
import { client } from './client.gen';
import type { ExportRenditionData, ExportRenditionErrors, ExportRenditionResponses, GenerateVariationData, GenerateVariationErrors, GenerateVariationResponses, GetJobStatusData, GetJobStatusErrors, GetJobStatusResponses, TaggedDocumentDetailsData, TaggedDocumentDetailsErrors, TaggedDocumentDetailsResponses, TaggedDocumentsData, TaggedDocumentsErrors, TaggedDocumentsResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Tagged documents
 *
 * This API retrieves a list of tagged documents that users can access, including those they own or those shared with them, along with relevant metadata. It supports pagination and sorting, allowing users to specify the starting point, define the number of documents to return, and choose the order in which to list them. The response includes the requested documents and their metadata.
 */
export const taggedDocuments = <ThrowOnError extends boolean = false>(options?: Options<TaggedDocumentsData, ThrowOnError>) => (options?.client ?? client).get<TaggedDocumentsResponses, TaggedDocumentsErrors, ThrowOnError>({
    security: [{ name: 'X-API-KEY', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/beta/tagged-documents',
    ...options
});

/**
 * Tagged document details
 *
 * This API retrieves details of the pages and tagged elements within a specified document. It returns a paginated list of the document's pages and metadata about each page. If the document has tagged elements, the API includes their respective details, such as size and position. If the document does not have tagged elements, it returns an empty array. The response includes pagination information to help users navigate the document’s pages. A maximum of 10 pages can be returned in 1 API call.
 */
export const taggedDocumentDetails = <ThrowOnError extends boolean = false>(options: Options<TaggedDocumentDetailsData, ThrowOnError>) => (options.client ?? client).get<TaggedDocumentDetailsResponses, TaggedDocumentDetailsErrors, ThrowOnError>({
    security: [{ name: 'X-API-KEY', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/beta/tagged-documents/{documentId}',
    ...options
});

/**
 * Generate variation
 *
 * This API creates a document variation based on provided input parameters. After processing, it temporarily stores the generated document and makes it available to the user within a designated folder. The document remains accessible for 30 days, after which the system automatically removes it.
 */
export const generateVariation = <ThrowOnError extends boolean = false>(options: Options<GenerateVariationData, ThrowOnError>) => (options.client ?? client).post<GenerateVariationResponses, GenerateVariationErrors, ThrowOnError>({
    security: [{ name: 'X-API-KEY', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/beta/generate-variation',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Export rendition
 *
 * Export one or more pages from an Adobe Express document in supported formats. This endpoint accepts an export request and returns pre-signed URLs for accessing the rendered files.
 *
 * **Supported formats:**
 * - Image formats: `image/jpeg` (JPG), `image/png` (PNG)
 * - Video format: `video/mp4` (MP4)
 * - Document format: `application/pdf` (PDF)
 *
 * **Rendition availability:**
 * - Image renditions: Valid for 4 hours, maximum size 8192px on the longest side
 * - Video and PDF renditions: Valid for 24 hours, maximum size 4096px on the longest side
 *
 * **Asynchronous processing:**
 * Export requests are processed asynchronously. The response includes a `jobId` and `statusUrl` that you can use to track the export progress and retrieve the final rendition URLs.
 *
 * **Restrictions:**
 *
 * | Condition | Behavior | Error |
 * | --- | --- | --- |
 * | Public templates (Adobe-owned documents not copied to the user account) | Export is not allowed | `400 bad_request` |
 * | Document contains Adobe Stock–licensed assets | Export fails | `422 stock_content_detected` |
 */
export const exportRendition = <ThrowOnError extends boolean = false>(options: Options<ExportRenditionData, ThrowOnError>) => (options.client ?? client).post<ExportRenditionResponses, ExportRenditionErrors, ThrowOnError>({
    security: [{ name: 'X-API-KEY', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/beta/export-rendition',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Job status
 *
 * Retrieve a job's status by its `jobId`. Depending on the job type, the response may include job-specific details.
 */
export const getJobStatus = <ThrowOnError extends boolean = false>(options: Options<GetJobStatusData, ThrowOnError>) => (options.client ?? client).get<GetJobStatusResponses, GetJobStatusErrors, ThrowOnError>({
    security: [{ name: 'X-API-KEY', type: 'apiKey' }, { scheme: 'bearer', type: 'http' }],
    url: '/status/{jobId}',
    ...options
});
