import { describe, it, expect } from 'vitest';

describe('@musallam/indesign', () => {
  it('should export from flat', async () => {
    // Populated after `pnpm generate`
    expect(true).toBe(true);
  });
});
