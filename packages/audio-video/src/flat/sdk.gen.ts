// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client';
import { client } from './client.gen';
import type { AvatarsData, AvatarsErrors, AvatarsResponses, CancelRenderJobData, CancelRenderJobErrors, CancelRenderJobResponses, DubData, DubErrors, DubResponses, GenerateAvatarData, GenerateAvatarErrors, GenerateAvatarResponses, GenerateReframedVideoData, GenerateReframedVideoErrors, GenerateReframedVideoResponses, GenerateReframedVideoV2Data, GenerateReframedVideoV2Errors, GenerateReframedVideoV2Responses, GenerateSpeechData, GenerateSpeechErrors, GenerateSpeechResponses, GetPresetsData, GetPresetsErrors, GetPresetsResponses, JobResultV2Data, JobResultV2Errors, JobResultV2Responses, ListRenderJobsData, ListRenderJobsErrors, ListRenderJobsResponses, StatusData, StatusErrors, StatusResponses, TemplateDescribeData, TemplateDescribeErrors, TemplateDescribeResponses, TemplateRenderData, TemplateRenderErrors, TemplateRenderResponses, TranscribeData, TranscribeErrors, TranscribeResponses, VoicesData, VoicesErrors, VoicesResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Get available voices
 *
 * This endpoint provides the list of all available voices for the user's enterprise.
 */
export const voices = <ThrowOnError extends boolean = false>(options?: Options<VoicesData, ThrowOnError>) => (options?.client ?? client).get<VoicesResponses, VoicesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { name: 'x-api-key', type: 'apiKey' }],
    url: '/v1/voices',
    ...options
});

/**
 * Generate speech from text
 *
 * This endpoint generates speech from a transcript. You can provide the transcript either as plain text or a pre-signed URL. The response will include a job ID and a status URL for tracking the job.
 */
export const generateSpeech = <ThrowOnError extends boolean = false>(options: Options<GenerateSpeechData, ThrowOnError>) => (options.client ?? client).post<GenerateSpeechResponses, GenerateSpeechErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { name: 'x-api-key', type: 'apiKey' }],
    url: '/v1/generate-speech',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get job status
 *
 * Provides the status and result of an asynchronous job. For Dynamic Graphics Render (DGR), returns the status of a Describe template or Render job.
 */
export const status = <ThrowOnError extends boolean = false>(options: Options<StatusData, ThrowOnError>) => (options.client ?? client).get<StatusResponses, StatusErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { name: 'x-api-key', type: 'apiKey' }],
    url: '/v1/status/{jobId}',
    ...options
});

/**
 * Describe template
 *
 * Analyzes a MOGRT (video template) file and returns a manifest of editable controls; fonts, images, audio, video and other supported values.
 */
export const templateDescribe = <ThrowOnError extends boolean = false>(options: Options<TemplateDescribeData, ThrowOnError>) => (options.client ?? client).post<TemplateDescribeResponses, TemplateDescribeErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { name: 'x-api-key', type: 'apiKey' }],
    url: '/v1/templates/describe',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Fetch video rendering presets
 *
 * Returns a list of predefined social-first encoding presets for rendering outputs.
 */
export const getPresets = <ThrowOnError extends boolean = false>(options?: Options<GetPresetsData, ThrowOnError>) => (options?.client ?? client).get<GetPresetsResponses, GetPresetsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { name: 'x-api-key', type: 'apiKey' }],
    url: '/v1/presets',
    ...options
});

/**
 * Render template
 *
 * Renders one or more video variations by applying overrides and export presets. Submit up to 10 overrides per call for a subset of editable layers and get a pre-signed URL link to download the video file. For layers that are not editable, the system defaults are automatically applied at export.
 */
export const templateRender = <ThrowOnError extends boolean = false>(options: Options<TemplateRenderData, ThrowOnError>) => (options.client ?? client).post<TemplateRenderResponses, TemplateRenderErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { name: 'x-api-key', type: 'apiKey' }],
    url: '/v1/templates/render',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Cancel a render job
 *
 * Aborts an in-flight render job. Returns `202 Accepted` as soon as the cancellation has been accepted; subsequent calls to `GET /v1/status/{jobId}` will report `"status": "canceled"` once the worker has fully stopped the underlying render.
 *
 * **Applicable only to render jobs submitted via `POST /v1/templates/render`.** This endpoint does not apply to Describe, Reframe, TLS, TTS, or Avatar jobs.
 *
 * **Notes:**
 * - Cancellation is idempotent.
 * - Once propagated to the worker, in-progress outputs are not uploaded to the destinations specified in the original render request.
 * - For a short window after the PUT, the status endpoint may still report `running`; poll until it transitions to `canceled`.
 */
export const cancelRenderJob = <ThrowOnError extends boolean = false>(options: Options<CancelRenderJobData, ThrowOnError>) => (options.client ?? client).put<CancelRenderJobResponses, CancelRenderJobErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { name: 'x-api-key', type: 'apiKey' }],
    url: '/v1/cancel/{jobId}',
    ...options
});

/**
 * List render jobs
 *
 * Returns a paginated list of template render jobs owned by the authenticated caller, optionally filtered by status and creation date.
 *
 * **Applicable only to render jobs submitted via `POST /v1/templates/render`.** Jobs from other API types (Describe, Reframe, TLS, TTS, Avatar) are not returned.
 *
 * **Filter syntax (FIQL):**
 * - `status==running` — single status equality
 * - `status=in=(running,not_started)` — status set membership
 * - `createdDate=ge=2026-05-01T00:00:00Z` — absolute ISO 8601 lower bound
 * - `createdDate=ge=-P7D` — relative ISO 8601 duration (last 7 days)
 * - Combine with `;` (AND): `status==running;createdDate=ge=-P7D`
 *
 * **Default filter:** all statuses, `createdDate` within the last 30 days.
 *
 * **Retention:** `createdDate` filter values must fall within the 30-day retention window.
 *
 * **Pagination:** follow `paging.nextUrl` until it is absent (last page).
 */
export const listRenderJobs = <ThrowOnError extends boolean = false>(options?: Options<ListRenderJobsData, ThrowOnError>) => (options?.client ?? client).get<ListRenderJobsResponses, ListRenderJobsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { name: 'x-api-key', type: 'apiKey' }],
    url: '/v1/templates/render-jobs',
    ...options
});

/**
 * Reframe video
 *
 * This endpoint enables you to reframe video using AI. Provide video input with a pre-signed URL to generate reframed video output.
 *
 * This API has more limited capabilities than the v2 endpoint, and may be deprecated soon. **It's recommended to use the v2 endpoint instead.**
 */
export const generateReframedVideo = <ThrowOnError extends boolean = false>(options: Options<GenerateReframedVideoData, ThrowOnError>) => (options.client ?? client).post<GenerateReframedVideoResponses, GenerateReframedVideoErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { name: 'x-api-key', type: 'apiKey' }],
    url: '/v1/reframe',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Transcribe media
 *
 * Generates transcripts and captions for the input audio or video in the source language or in a target language.
 */
export const transcribe = <ThrowOnError extends boolean = false>(options: Options<TranscribeData, ThrowOnError>) => (options.client ?? client).post<TranscribeResponses, TranscribeErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { name: 'x-api-key', type: 'apiKey' }],
    url: '/v1/transcribe',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Dub audio or video
 *
 * Generate dubbed video or audio. A composited lip sync can also be added for video dubbing.
 */
export const dub = <ThrowOnError extends boolean = false>(options: Options<DubData, ThrowOnError>) => (options.client ?? client).post<DubResponses, DubErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { name: 'x-api-key', type: 'apiKey' }],
    url: '/v1/dub',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get available avatars
 *
 * Retrieves the list of all available avatars for the user's enterprise.
 */
export const avatars = <ThrowOnError extends boolean = false>(options?: Options<AvatarsData, ThrowOnError>) => (options?.client ?? client).get<AvatarsResponses, AvatarsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { name: 'x-api-key', type: 'apiKey' }],
    url: '/v1/avatars',
    ...options
});

/**
 * Reframe video v2
 *
 * This endpoint enables you to reframe the input media. You can provide a video input via a pre-signed URL to generate reframed output video.
 */
export const generateReframedVideoV2 = <ThrowOnError extends boolean = false>(options: Options<GenerateReframedVideoV2Data, ThrowOnError>) => (options.client ?? client).post<GenerateReframedVideoV2Responses, GenerateReframedVideoV2Errors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { name: 'x-api-key', type: 'apiKey' }],
    url: '/v2/reframe',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get job result
 *
 * This endpoint retrieves the result of an asynchronous reframed job initiated using the job ID.
 */
export const jobResultV2 = <ThrowOnError extends boolean = false>(options: Options<JobResultV2Data, ThrowOnError>) => (options.client ?? client).get<JobResultV2Responses, JobResultV2Errors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { name: 'x-api-key', type: 'apiKey' }],
    url: '/v2/status/{jobId}',
    ...options
});

/**
 * Generate avatar video from text
 *
 * Generates an avatar video from a provided transcript or audio file. You can provide the transcript either as plain text or a pre-signed URL. The API is asynchronous, so the response will include a job ID and a status URL for tracking the running job.
 */
export const generateAvatar = <ThrowOnError extends boolean = false>(options: Options<GenerateAvatarData, ThrowOnError>) => (options.client ?? client).post<GenerateAvatarResponses, GenerateAvatarErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { name: 'x-api-key', type: 'apiKey' }],
    url: '/v1/generate-avatar',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});
