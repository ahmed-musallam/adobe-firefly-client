import { describe, it, expect } from 'vitest';

describe('@musallam/substance-3d', () => {
  it('should export from flat', async () => {
    // Populated after `pnpm generate`
    expect(true).toBe(true);
  });
});
